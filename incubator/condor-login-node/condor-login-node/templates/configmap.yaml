apiVersion: v1
kind: ConfigMap
metadata:
  name:  {{ include "condor-login-node.fullname" . }}-config
  labels: 
    app: {{ template "condor-login-node.fullname" . }}
    chart: {{ template "condor-login-node.chart" . }}
    release: {{ .Release.Name }}
    instance: {{ .Values.Instance }}
data:
  tconfig-file.conf: |+
{{ .Values.LoginConfigFile | indent 4 }}
