kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: osg-frontier-squid-{{ .Values.Tag }}-claim
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-storage